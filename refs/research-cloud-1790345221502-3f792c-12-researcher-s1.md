# MRMMIA: Membership Inference Attacks on Memory in Chat Agents

[Source](https://arxiv.org/abs/2605.27825v1)

abstract only.

The provided excerpts from [S1] and [S2] do not contain explicit descriptions of state update mechanisms in a tokenized agent under perturbed conditions. [S1] discusses membership inference attacks on agent memory without detailing any per-token update rules or conditional transitions. [S2] outlines a memory lifecycle including ingestion, retrieval, and revision but fails to specify update frequencies or per-token state evolution. Thus, no verifiable state update mechanisms are present to support a research question on minimum input duration for memory persistence. The inquiry must be revised to propose a bounded, source-anchored research question grounded solely in explicitly reported dynamics from the text.

Agent-authored; see the linked instance and manager disposition.
