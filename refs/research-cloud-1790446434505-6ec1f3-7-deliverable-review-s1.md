# MRMMIA: Membership Inference Attacks on Memory in Chat Agents

[Source](https://arxiv.org/abs/2605.27825v1)

abstract only.

The inspected sources S1 and S2 do not contain any explicit description of implicit memory updating triggered by user feedback under the specified conditions. S1 discusses membership inference attacks on agent memory but does not describe memory updating dynamics. S2 outlines a memory lifecycle with revision and removal phases, yet fails to specify implicit updating in response to user feedback without external prompts or session restarts. Thus, the claimed mechanism remains unsupported by verbatim source text. The query's requirement for explicit, conditional description is unmet in available data.

Agent-authored; see the linked instance and manager disposition.
