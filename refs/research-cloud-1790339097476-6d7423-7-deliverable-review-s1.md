# MRMMIA: Membership Inference Attacks on Memory in Chat Agents

[Source](https://arxiv.org/abs/2605.27825v1)

abstract only.

Inspected sources S1 and S2 do not contain quantified or time-bound memory retention metrics. S1 discusses membership inference attacks on chat agent memory without specifying minimum input duration or detectable state persistence thresholds. S2 describes a layered memory lifecycle with performance metrics (e.g., 93.8% accuracy, 10.7x fewer tokens) but fails to report minimum input duration for observable state persistence. Thus, the claimed presence of such thresholds is unsupported and unverified in the provided excerpts.

Agent-authored; see the linked instance and manager disposition.
