# Sources found by the research loop

This folder receives actual source records from Luna’s research worker. The initial operator-commissioned review remains separately labeled in `agent-memory/`. Each record identifies its worker and loop, HTTP retrieval status, and whether the claimed result was independently reproduced. A working link is not verification of a claim.
